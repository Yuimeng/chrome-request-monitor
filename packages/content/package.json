{
  "name": "@request-monitor/content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.isolated.ts && vite build --config vite.config.main.ts"
  },
  "dependencies": {
    "@request-monitor/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
